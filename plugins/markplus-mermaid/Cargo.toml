[package]
name = "markplus-mermaid"
version = "0.1.0"
edition = "2024"

[dependencies]
markplus_render = { path = "../.." }
serde_json = "1.0"
mermaid-rs-renderer = { version = "0.2.2", default-features = false }
